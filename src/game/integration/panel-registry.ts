/**
 * Re-export from the authoritative world registry.
 * Replaces the initial bootstrap placeholder where panels had empty hotspots.
 */
export { panels, getPanel, panelHref } from "@/game/world/registry";
